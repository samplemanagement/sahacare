{
  "name": "sahacare-v0",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "check:syntax": "./scripts/check-syntax.sh",
    "check:files": "test -f index.html && test -f how-it-works.html && test -f admin.html && test -f styles.css && test -f script.js"
  }
}
